{% extends 'base.html.twig' %}

{% block title %}Confirmation de commande{% endblock %}

{% block content %}
    <h2>Confirmation de commande</h2>
    <p>
        Bonjour <strong>{{ order.user.firstname }} {{ order.user.lastname }}</strong>, <br> <br>
        Nous vous remercions pour votre commande <strong>n°{{ order.reference }}</strong>. <br>
        Une confirmation viens de vous être envoyer à l'adresse <strong>{{ order.user.email }}</strong>.
    </p>
    <hr>
    <p>Votre commande sera livré par <strong>{{ order.carrierName }}</strong> ) l'adresse suivante :</p>
    <p>{{ order.delivery|raw }}</p>
    <hr>
    <p>Pour suivre votre commande, rendez-vous dans votre <a href="{{ path('account_order') }}">compte</a></p>
{% endblock %}
